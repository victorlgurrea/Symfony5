{% extends 'base.html.twig' %}

{% block title %}Editar subasta{% endblock %}

{% block contenedor_principal %}
    <div class="row">
        <div class="col-6"><h1>Editar subasta</h1></div>
        <div class="col-6 text-right"><a class="btn btn-outline-info mt-2" href="{{ path('app_index') }}"><i class="fas fa-undo"></i></a></div>
    </div>

    {{ include('marcador/_form.html.twig', {'button_label': 'Guardar'}) }}

    

{% endblock %}
